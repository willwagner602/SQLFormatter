A simple formatter for SQL scripts.  It currently does
a decent first pass, but is not feature complete.  It probably
will not produce good product with nested subqueries and other
complex edge cases.